{
  "name": "2r1b",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-frontend": "cd src/frontend && npm run build",
    "build-frontend-linux": "cd src/frontend && npm run build-linux",
    "build-server": "npx tsc --outDir build/",
    "build": "npm run build-frontend && npm run build-server",
    "build-linux": "npm run build-frontend-linux && npm run build-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "chokidar": "^3.5.3",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "@types/compression": "^1.7.2",
    "compression": "^1.7.4",
    "express": "^4.18.1",
    "nanoid": "^3.3.4",
    "socket.io": "^4.5.1"
  }
}
